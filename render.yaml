services:
  - type: web
    name: hifdhscroll
    runtime: node
    plan: free
    region: oregon
    branch: claude/app-deployment-plan-g48tfj
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20.9.0
      - key: NODE_ENV
        value: production
